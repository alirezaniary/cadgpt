"""Rule sets. The only place a jurisdiction exists.

The product ships no building code. A jurisdiction is a set of loaded IDS files and
nothing else, which is what lets one engine serve tenants in different countries without
a fork. Nothing in this package may name a country, a code, or a clause.
"""
