"""Uploaded files. Every model that needs one points here rather than holding its own.

One table means one place where size limits, checksums, content types, storage layout and
retention are decided -- and one place to change when storage moves from a local disk to
object storage.
"""
