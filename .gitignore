__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Compiled message catalogues: generated from the .po files, which are the source.
*.mo

# Local environment. .env.example is the template that is committed.
.env
services/api/.env

# Uploaded models and generated static files.
services/api/mediafiles/
services/api/staticfiles/

# Node
node_modules/
dist/

# Editor
.idea/

# Built workbench exports. Each is ~8 MB and fully reproducible: the stories are code, so
# any dated build below is regenerated from its own commit. What is committed is the index
# (design-previews/_archive.md), not the bytes.
design-previews/_live/

# Local source corpus and generated payloads for the INBR regulations pipeline
# (feat/inbr-regulations-pipeline); curated metadata and schemas stay tracked.
/.cadgpt/inbr/
